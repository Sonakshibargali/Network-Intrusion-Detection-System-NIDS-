# 🛡️ NIDS Security Audit Project

> **A Network Intrusion Detection System (NIDS) designed for security auditing, featuring real-time traffic monitoring, ML-based anomaly detection, and a user-friendly dashboard.**

This project mimics an industry-standard mini-product suitable for security demonstrations, developed for the 3rd year B.Tech Security Audit curriculum.

---

## ✨ Features

-   **📡 Real-time Traffic Monitoring**: Capture and analyze network packets on the fly.
-   **🤖 ML-based Anomaly Detection**: Uses a **Random Forest classifier** trained on the **NSL-KDD dataset** to identify potential threats (DoS, Probe, R2L, U2R).
-   **📊 Interactive Dashboard**: Visualize network traffic, alerts, and system health in a modern UI.
-   **🔐 User Management**: Secure authentication system with role-based access.
-   **⚡ RESTful API**: Powered by **FastAPI** for high performance and easy integration.

---

## 🛠️ Tech Stack

| Component | Technologies |
| :--- | :--- |
| **Backend** | 🐍 Python, FastAPI, SQLAlchemy, Scapy, Pandas, Scikit-learn, Uvicorn |
| **Frontend** | 🌐 HTML5, CSS3, JavaScript (Vanilla) |
| **ML Models** | 🧠 Scikit-learn (Random Forest) |
| **Database** | 🗄️ SQLite (Default, scalable) |

---

## 📋 Prerequisites

Before you begin, ensure you have the following installed:
-   🐍 **Python 3.8** or higher
-   📦 **pip** (Python package installer)
-   🦈 **Traffic capture permissions** (may require **Npcap** on Windows or **libpcap** on Linux/macOS)

---

## 🚀 Installation & Setup

### 1. 📥 Clone the Repository
```bash
git clone <repository-url>
cd "Security audit project"
```

### 2. 🔌 Set up the Backend
Navigate to the backend directory and install the required dependencies.
```bash
cd backend
pip install -r requirements.txt
```

### 3. 🧠 Train the Machine Learning Model
Before running the backend, generate the model artifacts. This script downloads the NSL-KDD dataset and trains the model.
```bash
cd ../ml
python train_nids.py
```
> *✅ This will save `nids_rf.pkl` to the `models` directory.*

### 4. 🗄️ Initialize the Database
Create the initial database and the default admin user.
```bash
cd ../backend
python create_admin.py
```

---

## 🏃‍♂️ Running the Application

### 1. 🔥 Start the Backend Server
From the `backend` directory:
```bash
uvicorn app_main:app --reload
```
> The API will be available at `http://127.0.0.1:8000` 🌐

### 2. 🖥️ Launch the Dashboard
Open `frontend/index.html` in your web browser.

> **Tip:** For the best experience, serve the `frontend` directory using a simple HTTP server:
> ```bash
> cd ../frontend
> python -m http.server 5500
> ```
> Then open `http://127.0.0.1:5500` in your browser.

---

## 🎮 Usage

### 🔐 Login
Use the default admin credentials:
-   **Username**: `admin`
-   **Password**: `admin`

### 📊 Dashboard
-   **Overview**: View live system stats and flow counts.
-   **Traffic**: Monitor incoming flows and their predicted classification (🟢 **Normal** vs 🔴 **Attack**).
-   **Alerts**: Review historical alerts generated by the IDS.

---

## 📂 Project Structure

```bash
├── 🔌 backend/            # FastAPI application source code
│   ├── 📦 app/            # API routers, models, and schemas
│   ├── 🧠 ml/             # ML model integration in app
│   ├── 🚀 app_main.py     # Application entry point
│   └── 📜 requirements.txt
├── 🖥️ frontend/           # Web dashboard (HTML/CSS/JS)
├── 📊 ml/                 # Model training scripts and raw data
│   ├── ⚙️ train_nids.py   # Script to train and save the model
│   └── 💾 models/         # Saved model artifacts
└── 📝 docs/               # Documentation
```

---

## 📜 License

This project is intended for **educational and security audit purposes**.
